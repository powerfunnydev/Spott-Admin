export const ERROR = 'error'; // An error occured ...
export const FETCHING = 'fetching'; // Then we start fetching the data from the server.
export const LAZY = 'lazy'; // Initially no data is loaded.
export const LOADED = 'loaded'; // The data was fetched successfully.
export const UPDATING = 'updating'; // When there is already data present, but we want to update the data.
